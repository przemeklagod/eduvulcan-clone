/** `Description` is the subject (or role, e.g. "Wychowawca") this teacher is listed under for the period. */
export interface Teacher {
  Id: number;
  BoxId: string;
  Position: number;
  Description: string;
  Name: string;
  Surname: string;
  DisplayName: string;
}
